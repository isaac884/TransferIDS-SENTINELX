# Simulator Agent

Simulator execution is intentionally disabled for the MVP detection path.

The product must start empty and must not generate synthetic traffic, fabricated alerts, seeded events, or substitute detection data. Keep this folder only as documentation for a future replay tool that must be explicitly fed user-provided files.
