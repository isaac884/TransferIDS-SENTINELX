# System Architecture

The platform is split into a FastAPI backend, static frontend shell, optional ONNX/SHAP integrations, PostgreSQL storage, Redis/event-bus-ready messaging and Nginx reverse proxy.

